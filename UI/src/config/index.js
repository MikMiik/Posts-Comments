const config = {
    routes: {
        home: "/",
        login: "/login",
        posts: "/posts",
        postDetail: "/posts/:id",
        editPost: "/edit-post/:id",
        notFound: "*",
    },
}

export default config
