const config = {
    routes: {
        home: "/",
        login: "/login",
        posts: "/posts",
        postDetail: "/posts/:id",
        newPost: "/new-post",
        editPost: "/edit-post/:id",
        notFound: "*",
    },
}

export default config
